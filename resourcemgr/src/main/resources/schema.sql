create table if not exists Person (
                                    id int not null primary key auto_increment,
                                    name varchar(100)
                                   );